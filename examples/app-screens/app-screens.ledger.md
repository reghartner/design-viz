# App screens example provenance

- Source: `fixtures/source-screens.html`, authored specifically for this example.
- Capture: `build-example.mjs` uses the repository's pinned Playwright Chromium
  to capture each 390 × 844 screen into the spec. No external media is loaded.
- All names, screens, camera imagery and service behavior are illustrative.
  Hearth is a fictional interface; no product team design or real user data is used.
- Intended evidence: screen selection stays synchronized with authored steps,
  carries through a waiting beat, and follows success/failure paths independently.
- This does not establish Figma API access, actual camera timing or company-host
  deployment. Users replace the images with authorized product exports.
